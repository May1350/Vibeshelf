# pnpm 9+ requires `packages` to be defined when this file exists,
# even for a single-package repo. Empty list means "no workspace
# members" — the file's only purpose is `ignoredBuiltDependencies`.
packages: []
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
