# .github/workflows/ci.yml
name: CI

on:
  pull_request:
  push:
    branches: [main]

# Split concurrency: cancel stale PR runs but never cancel a main deploy mid-flight.
# A canceled `vercel deploy --prebuilt` may leave prod in a partial state.
concurrency:
  group: ${{ github.ref == 'refs/heads/main' && 'deploy-main' || format('ci-{0}', github.ref) }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  PNPM_VERSION: "9.12.0"
  NODE_VERSION: "22"

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile

  lint:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: "${{ env.PNPM_VERSION }}" }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint

  lint-neg:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: "${{ env.PNPM_VERSION }}" }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint:neg

  typecheck:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: "${{ env.PNPM_VERSION }}" }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck

  unit:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: "${{ env.PNPM_VERSION }}" }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test:unit

  db-integration:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: "${{ env.PNPM_VERSION }}" }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - uses: supabase/setup-cli@v1
        with: { version: latest }
      - name: Restore Supabase Docker image cache
        id: docker-cache
        uses: actions/cache@v4
        with:
          path: /tmp/supabase-images.tar.gz
          key: docker-supabase-${{ hashFiles('supabase/config.toml') }}
      - name: Load cached Docker images
        if: steps.docker-cache.outputs.cache-hit == 'true'
        run: gunzip -c /tmp/supabase-images.tar.gz | docker load
      - name: Start Supabase stack
        run: supabase start
      - name: Populate .env.test.local
        env:
          TOKEN_KEY: ${{ secrets.TEST_TOKEN_ENCRYPTION_KEY_V1 }}
        run: |
          set -euo pipefail
          SRK=$(supabase status -o json | jq -r '.SERVICE_ROLE_KEY')
          ANON=$(supabase status -o json | jq -r '.ANON_KEY')
          URL=$(supabase status -o json | jq -r '.API_URL')
          printf '%s\n' \
            "NEXT_PUBLIC_SUPABASE_URL=$URL" \
            "NEXT_PUBLIC_SUPABASE_ANON_KEY=$ANON" \
            "SUPABASE_SERVICE_ROLE_KEY=$SRK" \
            "TOKEN_ENCRYPTION_KEY_V1=$TOKEN_KEY" \
            "GITHUB_CLIENT_ID=test-client-id" \
            "GITHUB_CLIENT_SECRET=test-client-secret" \
            "CRON_SECRET=test-cron-secret" \
            > .env.test.local
          chmod 600 .env.test.local
      - name: Reset database (replays all migrations)
        run: supabase db reset --no-seed
      - name: Generate and diff database types
        # Soft-check: warn (not fail) when database.types.ts drifts from
        # the migrations output. The hard check is too noisy when a PR
        # adds migrations — the developer may not have Docker available
        # locally to regenerate. Post-merge, `pnpm db:types` refreshes
        # the file against the live schema.
        run: |
          set -euo pipefail
          pnpm exec supabase gen types typescript --local > /tmp/database.types.ts
          [ -s /tmp/database.types.ts ] || { echo "Generated types file is empty"; exit 1; }
          if ! diff -u lib/db/database.types.ts /tmp/database.types.ts; then
            echo "::warning::database.types.ts differs from 'supabase gen types --local'. Run 'pnpm db:types' locally and commit."
          fi
      - name: Run integration tests
        run: pnpm test:integration
      # Cache Supabase images AFTER tests so a slow save can't block test
      # signal. Filter to supabase-only images — saving every image on
      # the runner (Node, Postgres pre-installed, etc.) was hanging the
      # job for >15 min on cold cache.
      - name: Save Docker images to cache (first run only)
        if: steps.docker-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        timeout-minutes: 5
        run: |
          set -euo pipefail
          IMAGES=$(docker images --format '{{.Repository}}:{{.Tag}}' | grep '^public.ecr.aws/supabase' || true)
          if [ -z "$IMAGES" ]; then
            echo "No supabase images found to cache"; exit 0
          fi
          # shellcheck disable=SC2086
          docker save $IMAGES | gzip > /tmp/supabase-images.tar.gz
      - if: always()
        run: supabase stop

  e2e:
    needs: [setup, db-integration]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: "${{ env.PNPM_VERSION }}" }
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - uses: supabase/setup-cli@v1
        with: { version: latest }
      - run: supabase start
      - name: Populate .env.local
        env:
          TOKEN_KEY: ${{ secrets.TEST_TOKEN_ENCRYPTION_KEY_V1 }}
        run: |
          set -euo pipefail
          SRK=$(supabase status -o json | jq -r '.SERVICE_ROLE_KEY')
          ANON=$(supabase status -o json | jq -r '.ANON_KEY')
          URL=$(supabase status -o json | jq -r '.API_URL')
          printf '%s\n' \
            "NEXT_PUBLIC_SUPABASE_URL=$URL" \
            "NEXT_PUBLIC_SUPABASE_ANON_KEY=$ANON" \
            "SUPABASE_SERVICE_ROLE_KEY=$SRK" \
            "TOKEN_ENCRYPTION_KEY_V1=$TOKEN_KEY" \
            "GITHUB_CLIENT_ID=test-client-id" \
            "GITHUB_CLIENT_SECRET=test-client-secret" \
            "CRON_SECRET=test-cron-secret" \
            > .env.local
          chmod 600 .env.local
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm build
      - name: Start app server in background
        run: |
          pnpm start > /tmp/next-server.log 2>&1 &
          echo "SERVER_PID=$!" >> "$GITHUB_ENV"
      - name: Wait for server ready
        run: |
          kill -0 $SERVER_PID || { cat /tmp/next-server.log; exit 1; }
          npx wait-on http://localhost:3000 --timeout 120000
      - run: pnpm test:e2e
      - name: Cleanup
        if: always()
        run: |
          kill $SERVER_PID || true
          supabase stop

  # Production deploy is TAG-TRIGGERED, not auto-promoted on every main push.
  # This gives the operator a gate to run the manual migration workflow first.
  # See db-migrate-prod.yml below.
  deploy:
    needs: [lint, lint-neg, typecheck, unit, db-integration, e2e]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install -g vercel@latest
      # Use a project-scoped Vercel token (not a personal account token)
      # to limit blast radius — leaked token can only affect this project.
      - run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
